{
  "name": "sudsandshine-firebase",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only auth,firestore,storage \"node --test tests/rules/*.test.mjs\"",
    "test:rules:local": "node --test tests/rules/*.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
